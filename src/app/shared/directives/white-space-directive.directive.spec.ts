import { WhiteSpaceDirectiveDirective } from './white-space-directive.directive';

describe('WhiteSpaceDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new WhiteSpaceDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
